{
  "schema_version": "1.0",
  "seed_id": "cwe122-sandbox-strbuf-concat-001",
  "seed": 1,
  "cwe_id": "CWE-122",
  "vulnerability_class": "Heap-based Buffer Overflow",
  "description": "Insert a heap buffer overflow in strbuf_concat_new(): reduce the allocation from malloc((new_len+1)*sizeof(char)) by 1, causing an off-by-one overflow when the concatenated content is copied.",
  "mutation_strategy": "alloc_size_undercount",
  "target_pattern": {
    "pattern_type": "malloc_call",
    "require_function_scope": true,
    "min_candidate_score": 0.5,
    "constraints": {
      "size_expr_min_complexity": 1,
      "require_post_write_access": true,
      "exclude_size_of_only": true
    }
  },
  "source_constraints": {
    "file_patterns": ["*.c", "*.cpp"],
    "exclude_patterns": ["*test*", "*_test.*", "*mock*", "*stub*"],
    "max_targets": 1
  },
  "metadata": {
    "tags": ["heap", "overflow", "off-by-one", "sandbox-eval"],
    "difficulty": "easy",
    "notes": "Targets strbuf_concat_new() in strbuf.c — allocation undersized by 1, immediate memcpy overflow.",
    "references": ["https://cwe.mitre.org/data/definitions/122.html"]
  }
}
