{
  "schema_version": "1.0",
  "seed_id": "cwe476-sandbox-graph-print-graph-002",
  "seed": 2,
  "cwe_id": "CWE-476",
  "vulnerability_class": "NULL Pointer Dereference",
  "description": "Remove the null guard in graph_print(): the check 'if (!g) return;' prevents a NULL dereference of g->num_vertices. Removing it exposes a NULL dereference when the graph pointer is invalid.",
  "mutation_strategy": "remove_null_guard",
  "target_pattern": {
    "pattern_type": "null_guard",
    "require_function_scope": true,
    "min_candidate_score": 0.8
  },
  "source_constraints": {
    "file_patterns": ["*.c", "*.cpp"],
    "exclude_patterns": ["*test*", "*_test.*", "*mock*", "*stub*"],
    "max_targets": 1
  },
  "metadata": {
    "tags": ["null-deref", "guard-removal", "sandbox-eval"],
    "difficulty": "easy",
    "notes": "Targets graph.c:264 — inline guard 'if (!g) return;' followed by g->num_vertices dereference.",
    "references": ["https://cwe.mitre.org/data/definitions/476.html"]
  }
}
